After the introduction of hooks, we refactor all the code of the developed project in the previous module using hooks and converting classes to functions. Hooks were also introduced along with redux.
